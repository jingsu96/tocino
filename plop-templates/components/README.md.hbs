# {{ pascalCase name }}


## Install

| type | command                                        |
| ---- | ---------------------------------------------- |
| npm  | `npm install @tocino-ui/{{{ dashCase name }}}` |
| yarn | `yarn add @tocino-ui/{{{ dashCase name }}}`    |
| pnpm | `pnpm add @tocino-ui/{{{ dashCase name }}}`    |


## Usage

```jsx
import {{{ pascalCase name }}} from `@tocino-ui/{{{ dashCase name }}}`;

function App() {
	return (
		<{{ pascalCase name }} />
	);
}
```
