// paths
const BUILD_PATH = 'dist/';

// platforms
const WEB = 'web';

// themes
const DARK = 'dark';
const LIGHT = 'light';

// prefix

const PREFIX = 'tocino';

module.exports = {
  BUILD_PATH,
  DARK,
  LIGHT,
  WEB,
  PREFIX,
};
